# 마이가디언 v2 — 코덱스 작업 안내

## 헌법은 CLAUDE.md 하나다

이 저장소의 규칙은 **`CLAUDE.md` 전문**이다. 작업 전에 처음부터 끝까지 읽고 그대로 따른다.

**이 파일에 규칙을 옮겨 적지 않는다.** 2026-08-17까지 이 파일은 7월 20일자 CLAUDE.md 복사본이었고,
그 사이 확정된 것들이 전부 빠져 있었다 — 버건디 팔레트 개편, 카드-온-캔버스 폐기,
고객 데이터 종단간 암호화, 케어 지면 「잡지지 신문이 아니다」, 바우하우스 양식, 기사 넘기기,
그리고 **지면 이미지 저작권 원칙(통신사 사진 금지)**. 복사본은 반드시 어긋난다.

규칙이 바뀌면 CLAUDE.md만 고친다. 이 파일은 역할과 절차만 담는다.

## 역할

- Claude Code = 주 시공자
- Codex = 검수·검증 전담. 그리고 사용자가 요청하면 시안 경쟁의 별도 안(안B)을 낸다.
- 브랜치: `main`(검증 완료본) / `claude/*`(시공) / `codex/*`(검수)
- 머지 권한은 사용자에게 있다. 임의로 main에 넣지 않는다.

## 작업 전

1. `git status --short`, 현재 브랜치, 최근 커밋을 확인한다.
2. **`CLAUDE.md` 전문을 읽는다.**
3. `docs/current-status.md`, `docs/decisions.md`, `docs/handoff.md`를 읽는다 —
   직전에 다른 도구가 무엇을 했고 무엇을 남겼는지가 거기 있다.
4. 사용자 변경이나 관련 없는 미추적 파일을 임의로 수정·삭제하지 않는다.

## 작업 후

`docs/handoff.md`에 **무엇을 했고 · 무엇을 검증했고 · 무엇이 남았는지** 남기고 commit·push 한다.
이건 형식이 아니라 통신 수단이다 — 다음에 붙는 도구가 이 파일로 맥락을 이어받는다.
같은 파일을 두 도구가 동시에 만지지 않는다. 전환할 때 반드시 commit·push.
