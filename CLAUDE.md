# 마이가디언 v2 — 프로젝트 헌법

신한라이프 FC팀의 보험 업무 플랫폼. 이 문서의 원칙은 모든 작업에 우선한다.

## 플랫폼 지향점 (존재 이유, 2026-07-19 사용자 확정)

이 플랫폼은 안창민 개인 도구가 아니다. **산하 모든 팀원이 함께 쓰는 팀 플랫폼**이다.
그리고 **어떤 기기(데스크톱·노트북·태블릿·휴대폰)에서 접속하든 모든 업무를 할 수 있어야 한다.**
읽기만 되는 것이 아니라 고객 등록·수정·삭제, 일정 등록, 발행까지 전부 가능해야 한다.

- 모든 설계 판단은 이 지향점에 부합하는지로 검증한다. "총관리자 PC에서만 되는 기능"은 미완성이다.
- 개발(코드 작업)은 한 대에서 한다 — 현재 안창민의 업무용 노트북. 이건 개발 편의의 문제이며
  플랫폼 사용과는 별개다.
- 현재 1차(정적 + 로컬 폴더)는 이 지향점을 절반만 충족한다. 로컬 폴더 방식은 휴대폰·태블릿
  브라우저에서 동작하지 않으므로, 팀 공유 백엔드(2차 공사)는 선택이 아니라 지향점 달성의 필수 조건이다.
  설계: `docs/phase2-server-design.md`

## 언어 (최우선 규칙)

- 모든 응답·보고·커밋 메시지·문서는 한국어로 작성한다.
- 첫 문장부터 한국어다. 작업 시작 멘트, 도구 호출 사이의 짧은 진행 멘트, 계획 서술, 오류 보고까지 예외 없다.
- 하위 에이전트(Explore, Task 등)를 띄울 때도 프롬프트에 "응답은 한국어"를 명시해서 넘긴다.
- 영어는 코드·명령어·파일명·기술 고유명사에만 허용한다.

## 세션 운영

- 작업 시작 전 `docs/current-status.md`, `docs/decisions.md`, `docs/handoff.md`를 읽는다.
- 세션 종료 전 변경 내용·검증 결과·미완료 항목을 `docs/handoff.md`에 기록하고 commit·push한다.
- 도구 분업: 앱 세션 = 화면·디자인 전담 / 터미널 세션 = 데이터·파이프라인·자동화 전담. 같은 파일을 두 세션이 동시에 만지지 않는다. 작업 전환 시 반드시 commit·push.
- 협업 역할: Claude Code = 주 시공자, Codex = 검수·검증 전담. 브랜치는 main(검증 완료본) / claude/*(시공) / codex/*(검수), 머지 권한은 사용자에게.

## 아키텍처 — 순수 정적

- HTML + CSS + JS + 정적 JSON만 사용한다.
- 상시 서버·데이터베이스·로그인 기능 금지.
- 사용자 입력은 브라우저 내에서만 처리하며 외부 전송을 금지한다.
- 예외: 팀 공유 기능(고객 데이터·공지·일정·발행·브리핑 동기화)은 2차 공사에서 네이버
  클라우드(NCP) 백엔드로 구현한다 (2026-07-20 개정 — Cloudflare안 폐기, 지금은 손대지 않음).
- 2차 인증 원칙(2026-07-20 확정): 자체 회원가입·비밀번호 저장 금지. 인증은 구글 계정(OAuth),
  접근 권한은 총관리자의 팀원 승인(초대·직급 부여)으로만 열린다.
- 고객 데이터 서버화 원칙(2026-08-02 확정): **종단간 암호화 전제.** 서버에는 판독 불가능한
  암호문만 올린다 — 열쇠는 FC 기기에만 있고 서버·클라우드·유출자 누구도 내용을 읽지 못한다.
  **레코드 전체를 암호화한다** — 서버 스키마는 고객코드·소유 계정·갱신시각·암호문뿐이다.
  보장분석은 클로드코드·코덱스로 FC의 PC에서 도는 로컬 공정이므로 서버가 내용을 읽을 일이 없다.
  검색·정렬·통계는 기기에서 복호화 후 처리한다(수백 명 규모).
  **열쇠는 FC 개인 단위** — 고객은 담당 FC 한 명만 본다(인수인계·이관·조인웍도 동일).
  이관은 원 FC가 내려받아 폴더로 넘기고 새 FC가 다시 올리는 방식 — 공개키 교환을 만들지 않는다.
  팀 공유 화면(캘린더 등)에는 마스킹 표기(안*민 · 010-****-3016)만 내보낸다.
  설계: `docs/phase2-server-design.md` STEP 2.

## 디자인

- 컨셉: 금융권 최상위 업무 플랫폼의 절제된 UI. 기준 실물은 신한라이프 SOL FC — 일하는 화면의 밀도를 목표로 한다.
- 업무 화면은 정보 밀도 우선: 필터바(조건 셀렉트 + 조회) + 테이블 + 상세의 조회 화면 문법을 따른다. 홈은 랜딩이 아니라 대시보드다(실데이터 수치 타일·목록). 일정·캘린더는 독립 프로젝트 '하랑지점'(C:\projects\ourbranch) 소관으로 이관됐다(2026-07-31).
- 밀도는 실데이터로만 만든다. 가짜 수치로 채운 화면 금지. 데이터가 없으면 해당 구획 자체를 만들지 않는다.
- 단, SOL FC의 메뉴 비대화(39개)는 반면교사 — 메뉴는 9개(고객관리·브리핑 포함)를 상한으로 유지하고 화면 안을 깊게 만든다.
- 기준 기기 우선순위(2026-07-20 개정 — 설계사 전용 도구 기준): ① 노트북·데스크톱(1920px, 편집·주력)
  ② 태블릿(1024~1366px, 편집 보조) ③ 모바일(390px, 열람 중심 — 고객관리·발행 등 쓰기 작업은
  거의 하지 않으므로 읽기 동선을 우선 최적화하되, 기능 자체는 막지 않는다).
- 예외: `web/care/`와 `web/card/`는 모바일 퍼스트 유지 — 독자가 고객(카톡 유입)이다.
- 화면의 주인에 따라 문법을 가른다 (원칙):
  - 설계사 화면(플랫폼) = 디자인 + 실무 효율. 조회 밀도가 우선이다.
  - 고객 화면(care/card/r) = **잡지다. 신문이 아니다** (2026-08-03 사용자 확정 — 가장 중요한 구분).
    기준 실물은 포브스·매경이코노미류 경제지, 고급 의학 저널. 신문은 빨리 훑는 것이고 잡지는
    머물러 보는 것이다. 그래서 ① 여백을 아끼지 않는다 ② 이미지는 크다(본문 폭을 넘어도 좋다)
    ③ 페이지마다 리듬이 다르다(도입은 크고 조용하게, 본문은 읽히게, 마무리는 여운)
    ④ 골드·버건디를 지면을 아름답게 만드는 데 쓴다 ⑤ 균질한 회색 텍스트 덩어리가 이어지면
    그건 신문이다. **예뻐야 한다** — 정보 전달만으로는 부족하고 "잘 만들었네"가 나와야 한다.
    편집 문법: 제호·킥커·표제 위계·1단 본문 조판·pull quote·헤어라인·이미지 캡션·칼럼 박스.
    주의: 플랫폼 화면의 에디토리얼 괘선 문법(카드 폐기·밀도 우선)은 **업무 화면 전용**이다.
    그 문법을 고객 화면에 그대로 밀면 잡지가 신문이 된다.
    **균질하면 실패다** (2026-08-03 사용자 확정). 잡지는 템플릿이 아니라 매 호·매 지면이
    다른 물건이다. 글자 크기를 과감히 흔들고(본문 17px 옆에 80px 수치가 있어도 된다),
    색·밑줄·형광·색면을 강조 수단으로 자유롭게 쓰고, 칼럼마다 색이 달라도 되고, 서체가
    섞여도 된다 — 가독성만 지키면 된다. 금지는 하나뿐: 처음부터 끝까지 같은 톤으로 흐르는 것.
    조판은 내용을 따라간다. 본문을 읽고 **이 글의 정점이 어디인지 판단해 거기에 힘을 준다** —
    독자가 스크롤하다 멈추는 자리를 의도적으로 만든다. 플랫폼의 "색은 신호로만" 규칙은
    업무 화면 전용이며 발행물에는 적용되지 않는다.
    **발행물은 독립 브랜드다** (2026-08-03 사용자 확정). 고객은 마이가디언 플랫폼을 본 적이
    없으므로 색·서체를 플랫폼과 맞출 이유가 없다. 버건디·Paperlogy는 플랫폼 표준일 뿐이며,
    발행물은 자기 정체성을 갖는다 — 발행물다운 디자인이 최우선이고 가족 표준은 여기 적용되지 않는다.
    **케어 지면 양식 = 바우하우스** (2026-08-03 사용자 채택, 시안 경쟁 결과). 삼원색
    빨강 `#E63329`·파랑 `#005BBB`·노랑 `#F5C518`, 잉크 `#111111`, 종이 `#F4F1EA`.
    기하 산세리프 전용(세리프 안 씀), 원·사각·삼각을 정보 구조로, 비대칭 격자, 큰 색면.
    칼럼마다 전면 색면 진입 지면(포스터)을 둔다. 초안: `drafts/care-bauhaus.html`.
    **기사 넘기기** (2026-08-03 사용자 확정). 기사 셋을 한 판에 이어 붙이면 스크롤이
    끝없이 길어져 읽기 전에 지친다. 기사 하나만 펼치고, 기사 안은 세로로 읽되 다 읽으면
    옆으로 넘어간다 — 폰은 스와이프, 노트북은 좌우 화살표, 공통으로 기사 끝 넘김 막대와
    목차 바로가기. 화면 크기와 무관하게 같은 방식이다(데스크톱만 이어 읽기로 두지 않는다).
    폰 조판 기준은 540px — 요즘 큰 폰이 가로 480px까지 잡히므로 430px으로 끊으면 그 기기들이
    데스크톱 크기 글자를 그대로 맞는다.
    **훑어도 읽히게** (2026-08-03): 독자는 폰으로 열고 7,000~11,000자를 다 읽지 않는다.
    핵심 수치를 60~90px 대형 블록으로 뽑고, 문단마다 형광 강조를 넣고, 중요 문단은 색면 위에
    올리고, 소제목만 읽어도 흐름이 보이게 한다. 본문은 오히려 보통 굵기로 두어 강조가 도드라지게.
    훑는 독자와 정독하는 독자가 모두 이해하는 이중 구조가 목표다.
  - 목표 수준: 현존 업계 최고이자 앞으로도 최고 — 디자인과 가독성 모두. "이 정도면 됐다"는 없다.
- 케어센터 편집장 원칙: 발행인이 누구든 편집장은 항상 안창민이다 (표지·판권 명기, 팀원 호는 발행 OOO FC 병기).
- 팔레트(2026-08-02 개정 — 버건디 채택, OKLCH 회전으로 위계 보존 변환): 캔버스 `#f5f7fa`,
  카드 표면 `#ffffff`, 텍스트 잉크 `#172033`, 보조 `#596579`, 3차 `#8993a3`, 테두리 `#dfe4eb`
  (입력창 `#cbd3de`), 포인트 버건디 `#bc003a`(hover `#980130`, 연배경쌍 `#ffeff0`),
  사이드바 딥버건디 `#481820`(deep `#331016`), 로즈 액센트 `#e05c72`,
  상태색 지급=그린·부지급=레드·일부지급=앰버(연한 동색 배경 + 진한 동색 텍스트 쌍 — 부지급 레드는
  포인트 버건디와 혼동되지 않게 주의). 이 팔레트는 하랑지점·보장분석 파이프라인에도 공통 적용 예정.
- 장식 목적의 색상 사용 금지. 색은 신호로만 쓴다. 포인트 버건디는 주액션·활성·선택·링크에만. 골드 `#c9a14a`는 카톡카드·인쇄물·케어 지면 전용.
- 2-톤 구조: 딥버건디 아이콘 레일(92px) + 라이트 콘텐츠. 레일 활성 항목은 연한 로즈 면.
  브랜드명은 상단바가, 탐색은 레일이 맡는다(역할 분리, 2026-08-02 시안 경쟁 안B 채택).
- 면 구분(2026-08-02 전면 개편 — 에디토리얼 문법): **카드-온-캔버스 폐기.** 종이색 캔버스
  `#faf9f7` 위에 괘선으로만 구획한다. 굵은 괘선 2px(잉크) = 구획 시작, 헤어라인 1px = 항목 사이.
  흰 카드·라운드·그림자를 쓰지 않는다. 지표는 칸막이 격자 대신 조판(수치 크게, 라벨 작게)으로.
  근거: 카드 격자는 어떤 생성기든 내놓는 기본값이라 색·서체를 바꿔도 생성물 인상이 남는다.
  구현은 platform.css 맨 아래 "에디토리얼 통일" 오버라이드 블록 — 걷으면 종전 문법 복귀.
- 선택 표시는 좌측 3px 인셋 포인트 막대로 통일한다 (목록 선택·활성 탭 문법).
- 서체(2026-08-02 개정): 플랫폼 UI 전체는 Paperlogy 단일(폴백 Pretendard). 위계는 서체 교체가 아니라 크기·굵기·색으로만 만든다. 케어 지면(주간 안창민)의 서체 전환·가독성 개선은 별도 세션에서 진행 예정.
- Noto Serif KR은 케어센터 아티클 제목·카톡카드·인쇄물 전용. 플랫폼 UI(사이드바·제목·버튼 등)에 세리프 사용 금지.
- 화면 최상단은 3단 헤딩 대신 **한 줄 제호 조판**: 제목(좌) + 구획명(우) + 설명(아래 한 줄).
  8화면에 3단 헤딩이 반복되면 서식이 된다(2026-08-02 개편).
- 번호 라벨은 한 자리(1·2·3). 두 자리 영점 채움(01·02)은 금지 — 생성물 인상을 준다.
- 타이포 위계(2026-07-20 개정): 핵심 수치 27px 이상 굵게, 페이지 제목 24~29px 굵게(자간 -0.04em),
  섹션 제목 18~20px 굵게(자간 -0.035em), 본문 13px(행간 1.6 이상), 보조 라벨 11px 연회색,
  eyebrow 소형 라벨 10px 굵게(자간 +0.08em 이상) — 화면 최상단은 eyebrow → 제목 → 설명의 3단 헤딩.
- 한글 조판: 본문 word-break: keep-all, 수치는 tabular-nums 고정폭.
- 내비게이션: 데스크톱·태블릿은 좌측 사이드바(홈/고객관리/청구·지급/사례·판례/학습/브리핑/케어센터/카톡카드), 모바일에서만 햄버거로 축소. 전 화면 동일 메뉴. admin은 어디에도 미노출.
- 레이아웃: 검색·목록형 화면은 2단(좌 목록 + 우 상세) 구조 허용. 홈 최상단에 통합 검색창 자리.
- 여백(2026-07-20 개정): 여백 예산은 바깥에 몰아준다 — 페이지 외곽 24~30px, 섹션 사이 12~16px,
  카드 패딩 16~21px. 내부는 촘촘하게, 밀도는 실데이터로.
- radius(2026-07-20 개정): 카드·패널 6px, 버튼·입력창 4px, 배지·칩·탭 3px. 그라데이션 금지.
- 모션(2026-07-20 개정): 0.14초 색 전환(색·배경·테두리)만. 확대·눌림·형태 변화·그림자 변화 금지.
  진행바 등 상태 표시만 0.3초 허용. prefers-reduced-motion 존중, 무한 반복·장식 애니메이션 금지.
- 예외(2026-07-20 사용자 승인): 시작 스플래시 1건 — 홈 진입 시 로고 + 광택 스윕 1회,
  세션당 1회, 총 1.5초 이내, prefers-reduced-motion에서는 정적 표시. 이 안의 그라데이션은
  광택 표현에 한해 허용. 다른 화면·요소로 확장 금지.
- 예외(2026-08-02 사용자 승인): 인트로(intro.html) 배경 영상 1건 — 무음·자동재생·심리스
  반복재생 허용. prefers-reduced-motion에서는 포스터 정지 이미지로 대체. 파일은 저장소
  web/assets/에 두고(외부 핫링크 금지) 5MB 이하로 최적화. 다른 화면·요소로 확장 금지
  (홈 히어로 등은 별도 승인 필요).
- 강조 패널은 상단 3px 포인트 컬러 스트립으로 표시한다.
- 바로가기 설치: 설치 전용 화면(web/install.html)을 두고 기기별 안내(안드로이드·아이폰·데스크톱)와
  설치 상태를 한 화면에서 처리한다. 상단 진입점 유지.
- 다크모드는 CSS 변수 구조로 작성한다 (구현은 후순위, 구조만 마련).
- 인쇄 최적화 (A4/A3 출력 시 가독성 우선)
- 스크린샷 검증 해상도: 1920px / 1180px(태블릿) / 390px 3종.

## 표기 금지

- 이모지 사용 금지
- 영문 장식 표기 금지 (STEP/PHASE는 예외)
- AI 말투 금지 (예: "~해 드릴게요", "물론입니다")
- 은유 표현 금지

## 지면 이미지 — 저작권 원칙 (2026-08-03 확정)

고객에게 발송되는 자료이므로 저작권 사고가 회사 이름으로 번진다. 다음 순서로만 쓴다.

- **주력: AI 생성 이미지** (2026-08-03 사용자 확정). 본문에서 프롬프트를 자동으로 뽑아 생성한다.
  저작권 시비가 없고, 아트 스타일을 일관되게 잡으면 호를 거듭할수록 이 매체만의 그림이 쌓인다.
  잡지가 뉴스 사진 대신 일러스트를 쓰는 이유와 같다.
- **보조: 무료 스톡**(Unsplash·Pexels — 상업 이용·수정 허용), **공공누리·기업 뉴스룸**(출처 표기).
  생성으로 안 되는 실물 장면에 한한다.
- **금지: 통신사·언론사 사진**(연합·뉴시스 등). **AI로 변형해도 금지** — 원본 저작권이
  사라지지 않고 2차적저작물 작성권 침해가 더해진다. 워터마크 유무와 무관하다.
- 출처가 불분명한 사진은 쓰지 않는다.

## 데이터 신뢰성 — 절대 원칙

- 판례·사례를 절대 생성하지 않는다. `data/`의 실데이터만 표시하고, 없으면 "관련 사례 없음"으로 표시한다.
- 사건번호 없는 판례는 표시하지 않는다.
- 모든 사례·판례에 신뢰도 등급(검증완료/부분검증/미검증-참고용)을 필수로 표기한다.
- 도구 결과 화면에는 다음 문구를 고정 표시한다:
  "본 자료는 공개된 분쟁 사례 정보 제공이며 개별 지급 여부 판단이 아닙니다"

## 개인정보

- 고객 실명·연락처 등 개인정보는 이 저장소에 절대 저장하지 않는다.
- 고객은 코드(C-YYYY-NNN)로만 표기한다.
- 외부 반출 마스킹(2026-07-20 확정): 고객 정보가 FC의 관리 영역 밖으로 나가는 모든 산출물
  (공유 자료·조인웍 화면·보고서 등)에서 이름은 가운데 글자 마스킹(안창민 → 안*민),
  전화번호는 중간 자리 마스킹(010-xxxx-1234)으로 표기한다. 원문은 FC 본인 화면에서만.

## v1 계승 원칙

- 데이터는 추출만 한다. 변환·합산·매핑 금지. (예: 엑셀 생성은 AI 호출 없이 저장된 데이터의 포맷 변환만)
- 한번 만든 로직에 패치 위에 패치를 쌓지 않는다. 더러워지면 다시 짓는다.
- 세션 종료 전 반드시 commit·push 한다.

## 기능 추가 브레이크

- 기능을 추가하기 전에 반드시 `docs/legacy-operations.md`의 "버려도 되는 기능" 목록과 대조한다.
- 목록에 있는 기능(태그·계약증서·상세정보·등급·파이프라인단계·게임·위임장·추천관리 등)은 다시 만들지 않는다.

## 구조

```
web/        # 정적 사이트 (index, claims, cases, learn, care, card, r, admin)
data/       # 스키마·사례·사전 JSON
pipeline/   # 수집·태깅·리포트 파이프라인 (Python)
docs/       # 참조 문서 (legacy-operations.md 등)
scripts/    # Playwright 스크린샷 검증 (1920px/390px)
```
