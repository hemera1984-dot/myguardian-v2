# 3차 재심 결과

- 검수 대상: `claude/auto-improve`의 `fbb0f1392d5c9c5b81c4430806fa069298fafb19`
- 재심 범위: `findings-02.md`의 치명 2건과 중요 5건
- 검수 원칙: 회신서의 자체 판정을 그대로 수용하지 않고 데이터 재집계, 스키마 검증, 코드 추적, 로컬 화면 재현으로 교차 확인했다.

## 항목별 판정

### [치명 1 해소] 검증 보고서 밖 `검증완료` 차단

- 위치: `pipeline/build_case_index.py:61-88`, `data/cases/index.json`, 검증 보고서 3종
- 재현 방법: 서비스 색인의 `검증완료` ID 집합에서 `fss-verification.json`, `scourt-verification.json`, `tribunal-verification.json` 결과 ID 합집합을 독립적으로 차감했다.
- 검증 결과: 색인 383건의 등급 분포는 `검증완료 209 / 부분검증 147 / 미검증-참고용 27`로 일치했다. 검증 보고서는 금감원 129건, 법원 243건, 조세심판원 9건이며 고유 ID는 381개다. `검증완료` 209건 가운데 세 보고서 어디에도 없는 ID는 0건이었다. 조세심판원 9건은 모두 `미검증-참고용`이다.
- 안전장치 확인: 색인 생성기는 세 보고서 ID를 모두 집계하고, 보고서 밖 `검증완료`가 남으면 `미검증-참고용`으로 강등하면서 검증 메모를 기록한다. 2차의 배포 차단 사유가 해소됐다.

### [치명 2 해소] 신규 `profile.json` 스키마 정합성

- 위치: `web/customers/index.html:344-395`, `data/schema/customer.schema.json`
- 재현 방법: 신규 등록 코드가 실제로 직렬화하는 객체와 동일하게 `고객코드`, `담당FC`, `카테고리`, `등록일`, `customerName`, `phone`을 구성해 Draft 2020-12 형식 검사까지 포함한 `jsonschema` 검증을 실행했다.
- 검증 결과: `C-2026-001`, `valueup`, `2026-07-19` 및 이름·연락처가 든 표본이 스키마를 통과했다. 고객코드 패턴, 카테고리 열거형, 등록일 형식이 모두 맞고 이름·연락처는 비공개 고객 저장소 규격의 허용 필드로 명시됐다. 화면 읽기도 한글 카테고리 키와 기존 `category`를 함께 처리한다.
- 한계: 브라우저 보안 경계 때문에 자동화 세션에서 네이티브 폴더 선택기를 가짜 핸들로 대체할 수는 없었다. 다만 저장 직전 생성 객체와 `JSON.stringify` 대상 코드 자체를 사용해 스키마 적합성을 검증했으므로 이 항목의 파일 형식 결함은 해소된 것으로 판정한다.

### [중요 1 해소] 중복 그룹의 복수 쟁점 보존

- 위치: `pipeline/build_case_index.py:38-50`, `web/cases/index.html:79,145,192-193`
- 재현 방법: 원본 726건을 복합키로 다시 그룹화하고 원본 쟁점 순서를 색인의 `쟁점목록`과 전수 비교했다. 이어 사례 화면에서 `증여세·보험` 필터와 상세를 재현했다.
- 검증 결과: 복수 쟁점 그룹은 35개이며 `쟁점목록` 불일치는 0건이다. `증여세·보험` 필터는 5건을 반환했고, `서울행정법원-2015-구합-58560` 상세에는 `보험료납입 · 증여세·보험`이 함께 표시됐다.

### [중요 2 해소] 카톡카드 금액 오계산 방지

- 위치: `web/card/index.html:91-123`
- 재현 방법: 로컬 화면에 `상품A | 12,000원`, `상품B | -45,000원`, `상품C | 1.2만원`을 입력한 뒤 합계와 경고 상태를 직접 확인했다. 이어 `12,000원`과 `45,000원`만 입력해 정상 경로도 확인했다.
- 검증 결과: 문제 입력에서는 월 보험료 합계가 숨겨지고 형식 경고가 표시돼 종전의 `57,012원` 오계산이 재현되지 않았다. 정상 입력에서는 경고 없이 `57,000원`이 표시됐다. 허용되지 않은 금액 하나라도 있으면 합계를 표시하지 않는 보수적 동작이 적용됐다.

### [중요 3 해소] 고객 쓰기 부분 실패와 중복 실행 방지

- 위치: `web/customers/index.html:297-313,344-395`
- 재현 방법: 메모 저장과 신규 등록의 실행 잠금, 쓰기 순서, 예외 처리, 잠금 해제를 코드 경로별로 추적했다.
- 검증 결과: 메모 저장은 시작 시 버튼을 잠그고 `finally`에서 해제한다. 신규 등록도 같은 방식으로 연속 실행을 막는다. `profile.json` 또는 `memo.md` 쓰기가 실패하면 생성 고객 폴더를 `removeEntry(code, { recursive: true })`로 정리한 뒤 실패 처리하므로 불완전 폴더가 남는 기존 경로가 제거됐다.

### [중요 4 해소] 사례 모바일 필터 터치 영역

- 위치: 사례 화면 390px
- 재현 방법: 390×844에서 결과·쟁점·기관 선택과 초기화 버튼의 경계 상자 및 문서 가로 초과를 측정했다.
- 검증 결과: 네 요소 모두 높이 44px였고 문서 너비와 스크롤 너비가 같아 가로 스크롤도 없었다. 비교 확인한 학습 탭 버튼도 45px였다.

### [중요 5 해소] 현재 상태 문서 정합성

- 위치: `docs/current-status.md:7-25`
- 재현 방법: 상태표와 실제 색인·기능을 대조했다.
- 검증 결과: 사례 383건과 등급 분포, 복수 쟁점 보존, 고객 메모·신규 등록, 학습 사전 검색, 카톡카드 생성기 상태가 현재 구현과 일치한다.

## 잔여 발견사항

이번 재심 범위에서 남은 치명·중요 결함은 없다. `findings-02.md`에 별도 제안으로 남긴 후속 과제는 요청에 따라 재심 판정에서 제외했다.

## 총평과 병합 판정

2차 검수의 배포 차단 사유 2건과 중요 결함 5건은 대상 커밋에서 모두 해소됐다. 데이터 신뢰도 커버리지, 신규 고객 파일 계약, 고객 발송 카드의 금액 안전성이 독립 검증을 통과했다.

**배포 판정: 가능**

**`main` 병합 판정: 가능**

이 판정은 검수 대상 커밋 `fbb0f13`에 한정한다.
