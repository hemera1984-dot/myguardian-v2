__pycache__/
scripts/shots/
scripts/fixtures/
data/care/desk/
pipeline/naver-keys.json
pipeline/monitor.log
server/.env
*.db
*.db-wal
*.db-shm
media-local/
.omd/
drafts/

# 확인용으로 잠깐 두는 파일 — 저장소에 들어가면 안 된다
web/**/_tmp*
_tmp*

# 고객 실데이터 — 공개 저장소에 절대 들어가면 안 된다 (2026-08-31)
# 이 저장소는 공개다. 여기 잠깐 둔 것도 다른 세션의 git add -A에 쓸려 들어간다.
mg-clients/
mg-clients-*/
mg-clients-*.zip
*고객대장*
