# 리포트 파이프라인 (P1~P5)

고객 리포트(web/r/) 생성 파이프라인의 자리다. 단계 정의는 확정 전이며, P1~P5 각 파일은 뼈대만 두었다.

- p1.py — 입력 수집 (자리)
- p2.py — 데이터 정리 (자리)
- p3.py — 리포트 본문 구성 (자리)
- p4.py — 정적 HTML 출력 (자리)
- p5.py — 난수 URL 발급·목록 갱신 (자리)

원칙: 데이터는 추출만 한다. 변환·합산·매핑 금지. 고객은 코드(C-YYYY-NNN)로만 표기한다.
